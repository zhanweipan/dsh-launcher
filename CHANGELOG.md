# Changelog

## 0.3.1（2026-09-04）

修复 Windows 上一键部署 100% 失败的问题（#1，PR #5）。

### 修复
- **Node.js 安装目录补齐 -win-<arch> 后缀**：下载 URL、SHA256 校验、解压目录、旧版本清理全部按实际架构（x64 / arm64）对齐。此前内部引用 `node-v<版本>`（不含后缀），找不到 node.exe，`node -v` 校验输出为空、部署中断
- 修复连带 bug：旧版本清理逻辑会把刚解压的当前版本目录（`node-v<版本>-win-x64`）误判为残留删除，造成反复下载仍失败
- `node -v` 校验清空继承的 `NODE_OPTIONS`（环境变量污染会使 node.exe 拒绝启动、输出为空）并增加 15s 超时防挂起
- 存量兼容：已残留 `node-v<版本>-win-x64` 目录的用户升级后直接就绪，无需重新下载；兼容无后缀的手动放置目录
- 清理误提交进仓库的 `.ext-test` 浏览器测试残留并加入 `.gitignore`

### 验证
- typecheck（node + web）通过；新旧代码对照逻辑断言 15/15 通过（目录解析 / 清理不误删 / 下载与 SHA 按架构构建 / 存量场景直接治愈）
- 真实 node.exe 污染环境实测（NODE_OPTIONS 注入未知 flag）：修复前输出为空，修复后正常返回版本号

## 0.3.0（2026-08-22）

### 新增
- **接管模式下 DSH 版本管理**：检测系统 DSH 安装位置/权限，升级/降级系统安装的 DSH（必要时 UAC 提权），升级前自动备份、失败自动回滚
- **外部 DSH 实例管理**：检测活动端口上非本应用启动的 DSH，显示「外部实例」标识与进程信息（PID/启动时间/命令行），一键停止（先弹确认再强杀）；其他程序占用端口时给出区分提示

### 验证
- 外部实例检测真实链路验证通过（本机会话 DSH 被正确识别为外部实例）

## 0.2.3（2026-08-22）

定期维护 + UI 改版。

### UI
- Apple 风格改版：磨砂玻璃卡片（backdrop-filter）、大圆角、SF 风格字体栈、苹果蓝主色（#0071e3）、柔和阴影
- 深浅色主题跟随系统（prefers-color-scheme + antd darkAlgorithm），窗口背景同步避免闪色
- 侧边栏改为浅色磨砂，日志面板统一为终端深色风格

### 维护
- 升级 electron-updater 6.3.9 → 6.8.9（修复：跨域重定向泄露 token 头，高危）
- 升级 electron-builder 24.13.3 → 26.15.3（修复：AppImage 路径注入，高危）
- npm 安全审计改用官方源（淘宝镜像不支持 audit 接口）
- 验证 electron-updater/extract-zip 已正确打包进应用

### 风险提示（未解决项）
- Electron 33 → 43 落后 10 个大版本，需专项升级（涉及 electron-vite 联动）
- 无自动化测试；部署下载（Node.js 30MB）无重试
- 未做代码签名；react 18 / antd 5 待升级

## 0.2.2（2026-08-14）

修复自动更新链路。

### 修复
- 发布配置改为 `releaseType: release`：electron-builder 默认创建**草稿** Release，会导致自动更新无法解析 latest（表现为 406 / 无法找到版本）
- 更新检查与下载内置 3 次指数退避重试：应对 GitHub 加速代理（hosts 指向本地 Caddy）转发 release-assets CDN 的间歇性 502

### 验证
- 自动更新实测通过：旧版（0.2.0）→ 检查更新 → 发现 v0.2.2 → 下载 → 重启安装 → 版本更新成功 ✅

## 0.2.1（2026-08-14）

- 首次打通发布流程（GitHub Releases + latest.yml 上传）；v0.2.1 草稿已手动转正式

## 0.2.0（2026-08-14）

M3 里程碑：多实例管理、PATH 暴露 dsh 命令、模型/API Key 配置引导。

### 新增
- 多实例管理：配置多个 DSH 实例（名称/端口），切换制运行，旧数据自动迁移
- 「在终端使用 dsh 命令」开关：把自管 dsh 加入用户 PATH（保留注册表原始类型）
- 模型/API Key 配置引导：一键打开 DSH Web 界面 / DSH_HOME 目录 / 日志目录
- 启动前端口占用检测：端口被其它实例占用时给出明确提示

### 修复
- 版本列表对 DSH 预发布版（rc）的过滤错误

### 验证
- 覆盖更新测试通过（0.1.0 → 0.2.0，NSIS 向导覆盖安装，数据目录保留，接管模式不丢失）

## 0.1.0（2026-08-13）

M1 + M2：一键部署链路、启停/端口/日志、接管系统已有部署、版本管理、开机自启、应用自动更新。

### 新增
- 一键部署：自动下载安装 Node.js（官方 zip + SHA256 校验）、npm 安装 DSH，全程免管理员权限
- 一键启动/停止 DSH，监听端口设置，启动后自动打开浏览器
- 接管系统已有部署：检测到系统 Node.js + DSH 时一键切换为直接管理
- 版本管理：Node.js / DSH 一键升级、降级、切换（自动先停实例）
- 开机自启、npm 镜像切换
- 实时日志查看（过滤/导出）
- 应用自动更新（electron-updater + GitHub Releases）